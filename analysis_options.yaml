# Shared analysis configuration for every package in the workspace.
# Individual packages include this file and may only add to it.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/generated/**"
